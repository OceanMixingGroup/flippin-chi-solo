function [c2P, c2A] = hard_code_approx_coefs_fcs()
% function [c2P, c2Ap] = hard_code_approx_coefs_fcs()
%   Define the two approximate calibration coefficients needed in the FCS reduction scheme
    c2P = 76.7; % psi/V
    c2A = -3; % /V
end
